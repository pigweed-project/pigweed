This directory contains data that gets generated during the
process of the agent-driven changelog update:

* `data.toml` - The first draft of the changelog content.

* `next.json` - The next batch of data that the agent should operate on.
